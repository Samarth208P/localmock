[build]
  command = "pnpm build:web"
  publish = "apps/web/dist"

[build.environment]
  NODE_VERSION = "22"

# SPA fallback — serve index.html for client-side routes
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
